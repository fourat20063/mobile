<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SuppAnnonce">

    <TextView
        android:id="@+id/textViewTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Suppression Annonce"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"/>

    <!-- Petit titre pour email -->
    <TextView
        android:id="@+id/textViewIdA"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ID Annonce :"
        android:textSize="16sp"
        android:layout_below="@id/textViewTitle"
        android:layout_marginStart="20dp"
        android:layout_marginTop="20dp"/>

    <EditText
        android:id="@+id/editTextIdA"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/textViewIdA"
        android:layout_marginTop="20dp"
        android:hint="ID"
        android:textSize="16sp" />

    <Button
        android:id="@+id/btnRecherche"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/editTextIdA"
        android:layout_marginTop="20dp"
        android:layout_alignParentStart="true"
        android:text="Suppression"
        android:textColor="#FFFFFF"
        android:background="#00FF00"/>

    <Button
        android:id="@+id/btnRetour"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnRecherche"
        android:layout_marginTop="20dp"
        android:layout_alignParentEnd="true"
        android:text="Retour"
        android:textColor="#FFFFFF"
        android:background="#FF0000"/>

</RelativeLayout>